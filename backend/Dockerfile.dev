FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci

COPY . .

EXPOSE 4000

# El comando real lo define docker-compose.dev.yml (entrypoint con volúmenes).
CMD ["npm", "run", "dev"]
